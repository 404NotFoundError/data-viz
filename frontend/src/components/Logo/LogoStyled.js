import styled from 'styled-components';

const LogoStyled = styled.a`
    position: absolute;
    z-index: 10;
    top: 40px;
    left: 40px;
    outline: none;
    cursor: pointer;
`;

export default LogoStyled;